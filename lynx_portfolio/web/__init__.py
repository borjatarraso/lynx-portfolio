"""Static web-UI assets for the Lynx Portfolio Flask API.

Files:
    * ``index.html`` — single-page dashboard.
    * ``app.css`` — mobile-first responsive styling.
    * ``app.js`` — vanilla-ES2020 client that talks to ``/api/*``.

Served by :mod:`lynx_portfolio.api` at ``/web``. No build step — edit
the files directly and refresh the browser.
"""
